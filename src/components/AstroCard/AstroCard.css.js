import { makeStyles } from "@material-ui/core/styles";
const useStyles = makeStyles(() => ({
  mainCard: {
    width: "500px",
    maxWidth: "80vw",
  },
  title: {
    fontSize: 23,
  },
  pos: {
    marginBottom: 12,
  },
}));
export default useStyles;
